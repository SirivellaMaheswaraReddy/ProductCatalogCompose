<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.ProductCatalog" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">#FFFFFF</item>
        <item name="android:navigationBarColor">#FFFFFF</item>
    </style>

    <!-- Theme for direct splash loading -->
    <style name="Theme.AppSplash" parent="Theme.ProductCatalog">
        <item name="android:windowBackground">#FFFFFF</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
<!--        <item name="android:windowLayoutInStatusBar">true</item>-->
    </style>
</resources>
